#!/usr/bin/env ruby

gemfile_option = "--gemfile-lock #{ENV['BUNDLE_GEMFILE']}.lock" if ENV['BUNDLE_GEMFILE'].to_s != ''
command_to_execute = "bundle exec bundle-audit check #{gemfile_option} --update"
puts command_to_execute
system(command_to_execute) || raise('Error on bundle audit')
